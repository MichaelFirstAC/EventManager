-- Run this only if you are getting "Access Denied" errors. 
-- This is usually a one-time setup for your local server.

-- Fix local root permissions
-- ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'michael4291';
-- FLUSH PRIVILEGES;